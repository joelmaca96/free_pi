"""Aplicación 2 — Backend API REST de solo lectura (FastAPI).

Expone los 19 endpoints del §5.1 de `doc/arquitectura/01_design.md` leyendo la
misma BD SQLite a través del dominio compartido `packages/baskonia_core`.
Streamlit sigue siendo la UI y no se toca en esta fase (F3 de la migración).
"""
