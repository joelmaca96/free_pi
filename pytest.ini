[pytest]
# Añade la raíz del proyecto al sys.path para que los imports
# (config, db, scraper, stats, insights, main) funcionen sin instalar el paquete.
pythonpath = .
testpaths = tests
# Los tests no deben tocar la BD real de data/baskonia.db
addopts = -q
